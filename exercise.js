// function declaration

function hello(name) {
    
}

// function expression
const hello = (name) => {};

const hello = function (name) {};

// arrow function
(first) => {
    Second;
};"

// object

let animal = {
    name: "
}
